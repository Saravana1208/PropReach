import React from 'react';

export function AnnouncementBar() {
  return (
    <div className="bg-brand text-[#1C1917] text-center py-2 px-4 text-sm font-semibold z-50 relative font-mono tracking-tight">
      Property growth, with a little more signal.
    </div>
  );
}

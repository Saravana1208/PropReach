import React from 'react';

export function AnnouncementBar() {
  return (
    <div className="bg-primary text-background text-center py-2.5 px-4 text-xs font-semibold z-50 relative uppercase tracking-widest">
      Property growth, with a little more signal.
    </div>
  );
}
